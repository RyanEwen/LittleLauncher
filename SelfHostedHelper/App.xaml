<Application x:Class="SelfHostedHelper.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:mica="clr-namespace:MicaWPF.Styles;assembly=MicaWPF"
             StartupUri="MainWindow.xaml"
             ShutdownMode="OnExplicitShutdown">
    <!--
        App.xaml
        ========
        Bootstrap the application resources:
          - WPF-UI theme dictionaries (Fluent Design controls)
          - MicaWPF theme dictionaries (Mica backdrop support)
          - Default English localization strings
          - Custom style overrides
        
        ShutdownMode is OnExplicitShutdown so the tray icon can keep
        the app alive after the settings window is closed.
    -->
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Unknown"/>
                <ui:ControlsDictionary />
                <mica:ThemeDictionary Theme="Auto" DesignModeTheme="Auto"/>
                <mica:ControlsDictionary />
                <ResourceDictionary Source="Resources/Localization/Dictionary-en-US.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Override default NavigationView background to transparent -->
            <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
            <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
        </ResourceDictionary>
    </Application.Resources>
</Application>
